<% provide(:title, "New template") %>
<h1>New template</h1>

<%= form_for(@request_type) do |f| %>
  <% if @request_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request_type.errors.count, "error") %> prohibited this request_type from being saved:</h2>

      <ul>
      <% @request_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :template %><br />
    <%= f.text_area :template, value: RequestType.find_by_name('Generic').template %>
  </div>
  <div class="actions">
    <%= f.submit "Save Template" %>
  </div>
<% end %>

<%= link_to 'Back', request_types_path %>
